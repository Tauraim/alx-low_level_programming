#include <stdio.h>

/**
 * main - will print out function output
 *
 * Return: will return zero if success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
