#include <stdio.h>

/**
 * main - print alphabet letter except q and e.
 * Return: always return zero if function runs successful
 */
int main(void)
{
	int n = 97;

	while (n <= 122)
	{
		if (n == 101 || n == 113)
		{
			n++;
			continue;
		}
		putchar(n);
		n++;
	}
	putchar('\n');
	return (0);
}
