#include "main.h"
/**
 * mul - function to multiply integers for resuilt
 * @a: variable declare
 * @b: variable declare
 * Return: zero if success
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
