#include "main.h"
/**
 * _isdigit - functions to check digit zero to nine
 * @c: int to be checked
 * Return: one if c is a digit and zero if otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
